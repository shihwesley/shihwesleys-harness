---
name: research
description: "Research any topic using the neo-research pipeline. Builds question trees, discovers and fetches sources, indexes into .mv2 stores, and distills into expertise artifacts. Use when user says /research, wants to learn about a library, framework, or topic before implementation."
argument-hint: "<topic> | load <topic> | list"
---

# /research alias

This is a convenience alias. Invoke the `neo-research:research` skill with these arguments:

$ARGUMENTS

Use the Skill tool: `Skill(skill="neo-research:research", args="$ARGUMENTS")`
